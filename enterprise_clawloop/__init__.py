"""Aganthos enterprise — proprietary learning algorithms. NEVER published.

This directory contains enterprise-only code that is never synced to
the public aganthos/clawloop repo. See resources/AGENTS.md for boundary rules.

Future structure (created when first backend is built):
  enterprise_clawloop/evolution/interfaces/  — Evolver protocol, PatchSet, TraceSchema
  enterprise_clawloop/evolution/backends/    — reflector, guided_mutation, dgm_h (hyperagent)
  enterprise_clawloop/evolution/core/        — archive, selection, evaluation, lineage
  enterprise_clawloop/cloud/                 — API server + async improvement workers
"""
